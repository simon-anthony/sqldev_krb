# Generated by krb_conf
[domain_realm]
        .example.com = EXAMPLE.COM
        example.com = EXAMPLE.COM

[libdefaults]
        default_realm = EXAMPLE.COM
        default_ccache_name = FILE:%{LOCAL_APPDATA}\krb5cc_%{username}
        default_client_keytab_name = FILE:%{LOCAL_APPDATA}\krb5_%{username}.keytab
#        verify_ap_req_nofail = false
#        dns_lookup_kdc = false

[realms]
        EXAMPLE.COM = {
            admin_server = win-bts6cdsef76.example.com
            kdc = win-bts6cdsef76.example.com
            kpasswd_protocol = SET_CHANGE
            # pkinit_anchors = DIR:/etc/certs/CA
            pkinit_eku_checking = kpServerAuth
            pkinit_kdc_hostname = win-bts6cdsef76.example.com
        }
